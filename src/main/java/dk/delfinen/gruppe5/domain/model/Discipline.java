package dk.delfinen.gruppe5.domain.model;

//Har valgt Enum fordi: en datatype, der bruges til at definere en fast liste af værdier. Da den er som en klasse, men den indeholder kun et begrænset sæt konstante værdier.

public enum Discipline {
    Butterfly,
    Crawl,
    Rygcrawl,
    Brystsvomning
}
